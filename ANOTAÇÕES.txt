USE O COMANDO ( isdigit ) para chegar se um imput ou dado de entrada
é um numero!

ex: variavel = input('digite um numero')
    if variavel.isdigit():
        print('vc digitou um numero')
    else:
        print('vc nao digitou um numero)

----------------------------------


impar ou para

para eu checar eu uso o comando ==
ex:
    par_impar = numero % 2 == 0

nesse comando a variável par_impar é o resultado
da operação % , esse comando % resulta no resto 
da divisão, e logo em seguida o comando == 0 checa
se o resto da divisão é 0, caso for verdadeiro
seguinifica que o numero é par!!!

use o try e execpt no lugar do if e else,
pois é mais simples e rapido para condições.


----------

comando len conta quantos digitos tem uma string 
e retorna um resultado com numero inteiro.
ex: variavel = len(nome)

--------

fatiamento de string: usando o comando [1]
ex: nome = jonathan
    print(nome[1])

nesse exemplo será mostrado somente 1 caractere 
contando do indice 1 que é a letra n.

ex 2: nome = jonathan 
      print(nome[:3])

esse codigo imprime uma sequencia de 3 caracteres
da string na variavel.

ex 3: nome = jonathan
      print(nome[4:])
    
dessa forma imprime sequencia de 4 caracteres
de tras para frente.

---------------------

funções =

.lower() #converte a string para letra minuscula / retorna valor STRING
.startwith() #verifca qual valor começa, e retorna um valor boleano = True or False
.endwith() #verifca qual valor termina e retorna um valor boleano



LIMPAR TERMinal  / import os
comando os.system('cls')

range(5) 
- range(5) generates numbers from 0 to 4.
- You can also use range(start, stop, step) for more control.

range(1, 10, 2)
print = 1 


.replace(',', ' ') funçao para converter uma string em outra string

outra forma de usar esse metodo é usar a expressao regular


tem que importar 
import re

nome = re.sub(
    r'[^0-9]',  #^ quer dizer que tudo não for numero u inteiro, tira
    '',  # mundando para isso aki, que é nada
    'nome' # imput pelo cliente
)